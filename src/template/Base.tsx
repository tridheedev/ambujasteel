import { Meta } from '@/Layout/Meta';
import React from 'react';

type Props = {};

const Base = (props: Props) => {
  return (
    <div>
      <Meta title='app' description='disc' />
    </div>
  );
};

export default Base;
